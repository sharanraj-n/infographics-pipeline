import json

class MergerAgent:
    """Integrates content and visual layout coherently."""

    def merge(self, content_data, design_spec):
        final_design = {
            "merged_layout": {
                "content_blocks": content_data,
                "design_guide": design_spec,
                "layout_style": "balanced composition"
            }
        }
        return final_design
